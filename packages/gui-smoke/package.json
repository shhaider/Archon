{
  "name": "@archon/gui-smoke",
  "version": "0.3.10",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "playwright test",
    "smoke:headed": "playwright test --headed",
    "smoke:report": "playwright show-report",
    "smoke:install": "playwright install --with-deps chromium",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0"
  }
}
